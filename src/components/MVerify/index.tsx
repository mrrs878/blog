import React, { useState } from 'react';

const MVerify = () => {
  const [positionX] = useState(0);
  return (
    <div className="container" style={{ width: '400px', height: '200px', border: '1px solid #ccc' }}>puzzle</div>
  );
};

export default MVerify;
